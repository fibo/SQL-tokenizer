{
  "name": "sql-tokenizer",
  "version": "0.0.0",
  "description": "analyzes SQL and converts it into a list of tokens",
  "main": "src/index.js",
  "scripts": {
    "flow": "flow",
    "lint": "standa",
    "postversion": "npm publish; git push origin v${npm_package_version}; git push origin master",
    "test": "tape -r babel-register test/*.js"
  },
  "pre-commit": [
    "lint"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SQL-JSON/tokenizer.git"
  },
  "keywords": [
    "SQL",
    "tokenizer"
  ],
  "standa": {
    "parser": "babel-eslint",
    "plugins": [
      "flowtype"
    ]
  },
  "author": "Gianluca Casati (http://g14n.info)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/SQL-JSON/tokenizer/issues"
  },
  "homepage": "https://github.com/SQL-JSON/tokenizer#readme",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^8.2.2",
    "babel-preset-flow": "^6.23.0",
    "babel-register": "^6.26.0",
    "dot-editorconfig": "^1.1.0",
    "eslint-plugin-flowtype": "^2.46.1",
    "flow-bin": "^0.66.0",
    "pre-commit": "^1.2.2",
    "standa": "^2.0.0",
    "tape": "^4.9.0"
  },
  "dependencies": {
    "sql92-operators": "^1.0.0"
  }
}
